# CONTEXT — folk.tw 的領域詞彙

這裡記**本站在講民俗時，每個詞精確指什麼**，以及哪些相近的詞不可互換。

🔴 **收錄判準：只記「弄錯會產生錯誤陳述」的詞。** 一般名詞不收；能從程式碼一眼看懂的
不收。每一條都要指出**判定的唯一入口在哪個檔**——詞彙的權威在程式碼，這裡只是索引。

> 建於 2026-08-20 的架構巡檢。當時六個新模組各自定義或釐清了一個詞彙，
> 而這些區別原本只活在檔頭註解裡、沒有一個地方能一次看完。

---

## 舉辦週期（cycle）

一個民俗活動多久辦一次。三種，**不可互推**：

| 值 | 意思 |
|---|---|
| `annual` | 每年 |
| `n_year_ke` | 逢特定**地支年**舉行的香科／醮典 |
| `irregular` | 不定期 |

🔴 **週期年數不可由地支數量推算。** 馬鳴山逢寅、午、戌年舉行，算術會得出「四年一科」，
但通行稱法與該筆自己的資料都是「**五年一科**」。民俗稱法不照算術走——
**來源怎麼說就怎麼寫，沒說就留空**。

🔴 **一個欄位只裝一種東西。** 舊的 `ke_rule` 同時裝地支（`丑辰未戌`）與自由文字
（`三年一科`），頁面樣板串接它，線上輸出過「三年一科**年一科**」這種病句。
已拆成 `ke_branches`（只放地支）／`ke_period_text`（週期怎麼講）／`ke_note`（補充）。

**唯一入口**：`src/lib/event-cycle.ts`（判定、造句、短標都在那裡）

## 香科

某些宮廟每隔數年舉行一次的大型遶境／醮典。「南瀛五大香」＝學甲香、麻豆香、蕭壠香、
西港香、土城香。

⚠️ **只有學甲香是四年一科**（逢子、辰、申年），其餘四香皆三年一科。
2026-08-20 站主裁示，與 nchdb 逐字記載一致。

⚠️ **香科與同名祭典可能不同週期**。`學甲上白礁暨刈香`：上白礁謁祖**每年**舉行，
刈香（＝學甲香）**四年一科**。所以同一個活動頁的 `cycle` 是 `annual` 而
地方慶典那邊的 verdict 是 `n_year`，**兩者都對，不是矛盾**。
跨資料集一致性檢查對這類情形採具名豁免＋必須寫理由。

**唯一入口**：`src/lib/event-cycle.test.ts` 的 `SCOPE_EXEMPT`（豁免與理由）

## 曆別（calendar）

日期用哪一套曆法。四種：`lunar`（農曆）／`solar`（國曆）／`solar_term`（節氣）／
`hijri`（回曆）。

🔴 **回曆不換算**，一律回 `null`。⚠️ 區分兩種「沒有日期」：
回 `null` ＝**這個曆別我們不換算**；回 `{iso: null}` ＝可換算但窗口內沒有這一天
（如 02-29 非閏年）。

🔴 曆別混淆過去是結構上開著的坑——農曆與國曆的換算函式**簽章完全相同**，
傳錯會通過型別檢查並回一個看起來合理的錯日期。現已收斂成一個
discriminated union 與單一入口。

**唯一入口**：`src/lib/calendar-date.ts`

## 豐化內容（enrichment）／沿革區塊

**兩個不同的判斷，不可合併**：

- **豐化**（`hasEnrichment`）＝這頁值不值得被推薦／被爬：`history || website || founded`
- **沿革區塊**（`hasHistorySection`）＝要不要渲染那個區塊：`founded || history || main_festival`

⚠️ 沿革判準**刻意不含 `website`**：那個區塊實際渲染的就是那三個欄位，
併進 `website` 會讓只有官網的廟拿到一個只剩標題的空區塊。

**唯一入口**：`src/lib/temple-enrichment.ts`

## 來源型別（source type）

`sources[].type` 標示這筆來源是什麼性質。七值：
`book`／`temple`／`gov`／`web`／`field`／`paper`／`other`。

⚠️ **`web` 裡混著本站自己的頁面。** festivals.json 有一批 `ref` 指向 `folk.tw` 的
交叉引用（note 寫「本站◯◯資料入口」），用途是導覽入口而非舉證。
2026-08-20 站主裁示併入 `web`（此前它們自成一個 `site` 值）。

🔴 **所以「是不是同源」不可以看 `type`，要看網址。** 併入之後型別上再也分不出來，
而這個區別有實際後果：同源連結不該加 `nofollow`／`target="_blank"`
（2026-08-20 之前渲染端無條件當外部處理，等於對自家內部連結下 nofollow，
而且不會有任何 gate 紅燈——`check:anchor-text` 驗的是可見文字不是 `rel`）。

**唯一入口**：判定 `isSameOrigin()`（`src/lib/sources.ts`）；渲染 `src/components/Sources.astro`

## 基準日（build date）

「這次 build 用哪一天」。**兩個概念，不可混成一個**：

- `buildDate()` ＝我這次 build 用哪一天（掃原始碼的 gate 要這個）
- `distBuildDate()` ＝那份 dist 是用哪一天建的（比對產物的 gate 要這個）

跨午夜的 build 會讓兩者相差一天。混成一個會讓其中一邊的 gate 變錯——
2026-08-19 發生過假紅燈（build 台北 23:45 開始、00:11 跑 gate，兩間廟被誤判）。

**唯一入口**：`src/lib/build-date.ts`

## 逐字引用

引用權威來源的原文，**一個字不改寫**。授權條件一律是**標示資料來源連結**——
少了連結就是違反授權，而那是不會有任何錯誤訊息的違反，所以由 gate 驗
「連結真的渲染在頁面上」。

⚠️ 逐字引用只准掛已授權網域；未授權來源可引述但不可逐字。

**唯一入口**：`scripts/check-source-refs.mjs` 的 `VERBATIM_ALLOW`

## verified（農民曆）

農民曆結論分兩層：**核心層**（確定性公式與天文資料，`verified: true`）與
**進階層**（須依《欽定協紀辨方書》逐條考據，`verified: false` 不上架）。

⚠️ 進階層**不只給結論，必附出處與推導**——這是與市面農民曆 app 的分水嶺。
諸說不一時**並陳，不裁定**。

**唯一入口**：`src/lib/almanac/`（設計意圖見該目錄 README）

---

## 相關

- 架構詞彙（module／interface／depth／seam／adapter）與 domain 文件的消費規則：
  [`docs/agents/domain.md`](docs/agents/domain.md)
- 決策紀錄（為什麼**不**做某件事）：[`docs/adr/`](docs/adr/)
- 各主題的決策脈絡與陷阱：[`docs/decisions/`](docs/decisions/)

🔴 **這份與程式碼不一致時，信程式碼**，並回頭修這裡——詞彙的權威在各檔的檔頭註解。
