# 2026-08-11 大腦執行紀錄

## ① 昨日賭注勝負

- `scoreboard.json`：`bets` 共 12 筆，全數 `status: resolved`，無 `open` 賭注待核對。

## ② 今日訊號判讀

讀 `data/seo-daily/2026-08-11.json`（GA4/GSC 近 7 日資料窗，2026-08-01..2026-08-08）：

- **highImpZeroClick／strikingDistance 非廟宇候選**：`deities/wangye_wu`（吳府千歲生日 impr184）、
  `deities/xiqin`（西秦王爺聖誕 impr147）、`deities/wangye_zhu`（朱府千歲生日 impr127）、
  `festivals/zhongyuan`（中原普渡2026 impr110／中元節 2026 impr90）、`poems/liushi_jiazi-1`
  （日出便見風雲散 impr90）、`deities/wangye_chi`（池夢彪 impr69）、`allusions/wuhan_qizhu`
  （吳漢殺妻為母救主 impr87）——與昨日（2026-08-10）候選清單**完全相同**，昨日已逐一查證現有
  程式碼／模板：`deities/[id].astro` 的 `deityTitle` 模板、`festivals/[slug].astro` 的
  `campaignSeo.zhongyuan`、`poems/[id].astro` 的 title 皆已 100% 命中查詢字面，位置 5.6–11.1
  卡在頁一邊界，屬正常低 CTR，非標題/摘要缺口。今日重新逐一核對，判斷不變，不重複修。
  `wangye_chi`／`wuhan_qizhu` 也與 8/6–8/9 連續多日重複、非新事證。
- **cannibalization**：本日清單（東天宮／觀音堂／鎮安宮／關聖帝君廟等）皆為同名多間廟宇互搶
  查詢字面，屬站規明文禁止逐廟調整、禁廟對廟密網範圍，不動手。
- **`/festivals/` 節日模組**（當期第一優先，屬反思白名單非本層範圍）：`index.coverage` 顯示
  `qianggu`／`dizang`／`zhongqiu` 皆已轉為 `Submitted and indexed`（qianggu、zhongqiu 於
  8/11 當日新爬取），`jilong-zhongyuan` 仍 `Discovered - currently not indexed`
  （站規明載該頁 8/3 已試過補內鏈無效，不重複嘗試）；`september-solar-terms`／`kongzi-birthday`
  `Discovered - currently not indexed`、`kinmen-bo-bing` `URL is unknown to Google`——這三個是
  9 月第二批節日，非本週檢查點範圍，僅路過確認。8/16 曝光檢查點尚未到期。

## ③ 今日動手

**本日內容層 no-op**（連續第 6 日）：所有候選皆與昨日相同且已被既有通用模板覆蓋查詢字面，
找不到符合四條件（事實可查證＋白名單內＋非個位數樣本＋標題摘要未覆蓋）的目標。

- 改動檔案：無（僅本檔留痕）。
- 賭 query/page：無新賭注。
- 預期效果：無（no-op）。

## 技術細節

- 執行前 `git status` 為 clean，但 `origin/main..HEAD` 有 3 筆本機已 commit 但尚未 push 的變更
  （`691f2c4` 每日數據、`f996c2a` 時事祈福自動編排、`7bd92ef`／`9977fc7`／`a6813f3` 52 週文章草稿與
  年度清理指令，皆為其他自動化留下的既有 commit，非本次任務產物）。依
  `scripts/lib/skip-ci-suffix.sh` 判準：本機有未推送 commit 時**不可加 `[skip ci]`**，
  否則整個 push 會 0 run、這些 commit 靜默停在舊版不上線。故本次 no-op commit 訊息不帶 `[skip ci]`。
- gate：本日無內容改動，未跑完整 gate（無變更需要 build 驗證）。
- commit：`chore(seo): 2026-08-11 無動作`（只含本檔，不含 `[skip ci]`，理由見上）。
- push：`git pull --rebase origin main` → `git push origin main`（會一併帶上前述 3 筆既有 commit）。
- push 後查核本次 commit 與前述 3 筆是否都出現在同一個 deploy.yml run 內，且 headSha 為最新 commit。
- 通知搜尋引擎：略過（無頁面內容改動）。
