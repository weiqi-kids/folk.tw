# 2026-08-12 大腦執行紀錄

## ⚠️ 執行前發現：工作目錄卡在錯誤分支（已修復）

任務開始時 `/root/folk.tw` 停在 `agent/annual-topic-quality` 分支（至少已停留 2 天，
reflog 顯示 8/10、8/11、8/12 的每日數據／時事祈福自動 commit 都誤落在這條分支，
而非 `main`——`main` 本地端落後 origin 17 筆，但 origin/main 本身沒有掉資料，
只有今天 15:30 的每日數據 commit 卡在本機沒推上去）。同時該分支上有一大批
未提交的工作中變更（`docs/annual-*`、`docs/topic-drafts/*`、`scripts/check-*`、
`src/data/festivals.json`、`src/lib/festival-og.ts`、`src/pages/festivals/*`、
兩張新 OG 圖），檔案 mtime 精確對齊本次任務啟動時刻，判斷屬其他並行 session
正在進行的「年度主題」工作，非殘留垃圾。

`seo-brain.sh` 的殘留清理步驟對 `src/` 是 `git checkout` + `git clean -fdq`（無 stash），
無論本次是否動手都會在我結束後把上述 `src/` 下的未提交變更（含未追蹤的新檔）直接刪除。
為避免摧毀他人工作中的內容，處理如下：

1. `git stash push -u -m "annual-topic-quality-preexisting-2026-08-12-seo-brain-run"`——
   把整批未提交變更（含未追蹤檔）安全收進 stash，不觸碰任何內容判斷。
2. `git checkout main` → `git merge --ff-only origin/main`——切回正確分支並補齊本機落後的 17 筆。
3. `git merge --ff-only agent/annual-topic-quality`——只帶上今天卡住的 1 筆
   `chore(seo): 每日數據 2026-08-12 [skip ci]`（純資料 JSON，與 origin/main 是直線 fast-forward，
   無衝突風險），讓今天的資料紀錄回到 main。
4. 確認 `git status` 乾淨、`agent/annual-topic-quality` 分支本身與其 3 筆既有 commit 完全未被更動。

**待辦（需要人工）**：`stash@{0}`（label：`annual-topic-quality-preexisting-2026-08-12-seo-brain-run`）
是別人正在做的「年度主題」內容，未套用回去。哪個 session 在做這件事、要不要
`git stash pop`（或用 `git stash branch <新分支> stash@{0}` 更安全地在原始基準上復原）
需要用戶或該 session 自己決定，我不會代為套用或丟棄。同時，是誰／什麼流程把
`/root/folk.tw` 切到 `agent/annual-topic-quality` 且維持了兩天，根因未查（可能是人工
切換後忘記切回，也可能是另一支自動化沒有像 `folk-qiugian` 系列一樣改用隔離 worktree）——
建議之後也給「大腦／收集」這類直接操作 `/root/folk.tw` 的 cron 加一道「起跑前確認在 main」
的檢查，或仿 `/opt/folk-tw-cron/topical` 做法搬進隔離 worktree。

## ① 昨日賭注勝負

- `scoreboard.json`：`bets` 全數 `status: resolved`，`open` 賭注 0 筆，無需核對。

## ② 今日訊號判讀

讀 `data/seo-daily/2026-08-12.json`（GSC 範圍 2026-08-02..2026-08-09，與昨日資料窗相同——
GSC 有 2–3 天延遲，資料窗未動屬正常）：

- **strikingDistance／highImpZeroClick**：候選清單與昨日（2026-08-11）**幾乎完全相同**
  （`deities/wangye_wu` 吳府千歲生日 impr199、`deities/xiqin` 西秦王爺聖誕 impr136、
  `deities/wangye_zhu` 朱府千歲生日 impr116、`festivals/zhongyuan`「中原普渡2026」impr112／
  「中元節 2026」impr90、`poems/liushi_jiazi-1` 日出便見風雲散 impr87）。這批已連續多日
  （8/6 起）逐一查證：`deities/[id].astro`、`festivals/[slug].astro`、`poems/[id].astro`
  既有模板皆 100% 命中查詢字面，位置卡在 5.6–11 屬頁一邊界正常現象，非標題/摘要缺口，
  今日重新核對判斷不變，不重複修。
- **cannibalization**：本日清單（東天宮／觀音堂／鎮安宮／關聖帝君廟等）皆為同名多間廟宇
  互搶查詢字面，屬站規明文禁止逐廟調整、禁廟對廟密網範圍，不動手。
- **`/festivals/` 節日模組**（當期第一優先，屬反思白名單非本層範圍）：8/16 曝光檢查點
  尚未到期，僅路過確認 `festivals/zhongyuan` 曝光持續（見上），未見新事證需要提前處理。
- **GA4 目標**：`audienceGoal` 8/13 檢查點目標 6,100，目前 `active28DayUsers` 已達 7,747，
  超前進度，非需要介入的訊號。

## ③ 今日動手

**本日內容層 no-op**（連續第 7 日）：所有候選皆與昨日相同且已被既有通用模板覆蓋查詢字面，
找不到符合四條件（事實可查證＋白名單內＋非個位數樣本＋標題摘要未覆蓋）的目標。

- 改動檔案：無新增內容改動（僅本檔＋分支修復留痕）。
- 賭 query/page：無新賭注。
- 預期效果：無（no-op）。

## 技術細節

- 分支修復細節見本檔最上方。
- `git rev-list --count origin/main..HEAD`（commit 前）＝1（今天稍早卡住的每日數據 commit）
  → 依規則本次 commit **不可加 `[skip ci]`**。
- gate：本日無內容改動，未跑完整 gate；分支修復純屬把 main 同步到 origin/main 既有內容
  ＋帶回今天已產生的資料 JSON，未產生新內容，無需 build 驗證。
- commit：`chore(seo): 2026-08-12 無動作（另修復工作目錄分支卡誤）`（不帶 `[skip ci]`）。
- push：`git pull --rebase origin main` → `git push origin main`。
- push 後查核 `deploy.yml` 是否有對應本次 head SHA 的新 run。
- 通知搜尋引擎：略過（無頁面內容改動）。
